<?xml version="1.0"?>
<globals>
    <global id="srcOut" value="${srcDir}/${slashedPackageName(packageName)}" />
    <global id="resOut" value="${resDir}" />
    <global id="packageName" value="${packageName}" />
    <global id="tableName" value="${tableName}" />
    <global id="rowsList" value="${rowsList}" />
</globals>
